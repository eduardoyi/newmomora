# Claim matrix

**No claim in this document is labelled `runtime-observation` or `production-confirmation`.
No build was run and no login was performed in this pass — every row below is
`code-evidence`, `config-evidence`, or `doc-evidence` only.** Doc-evidence rows point at a
feature doc under `docs/features/`; the underlying code paths those docs themselves cite
were spot-checked (see the "spot-check" note per section) but not exhaustively re-read line
by line for every claim.

Evidence class definitions used below: `code-evidence` (a source file was read directly),
`config-evidence` (a config file — `eas.json`, `app.json`, `package.json` — was read
directly), `doc-evidence` (a `docs/features/*.md` file was read; it in turn documents code
the doc's own author read), `runtime-observation` (not used here), `production-confirmation`
(not used here).

## Never-claim section (read first)

| Claim that must NEVER appear in copy or screenshots | Verdict | Evidence | Evidence class |
|---|---|---|---|
| Global search / "find any memory instantly" | FALSE as a public claim | `docs/features/memories.md:42-48` — a search hook (`useMemoriesSearch`) and a real Postgres full-text-search service function exist, but "Not reachable from any current UI (`timeline.tsx` has no setter for its search query)." | doc-evidence |
| "Looking Back" recap / resurfacing feature | NOT RELEASED | `docs/features/looking-back.md:3` — `**Status:** in-progress`. Also: `back-on-timeline.png` (repo root) visibly shows the Looking Back carousel and `marketing/assets/ugc/demo-cuts/demo-looking-back.mp4` is a video of the same sequence (per its own `README.md`: "The Looking Back sequence appears around 00:07–00:10") — **both assets must be excluded from any v2 frame.** | doc-evidence + config/asset inspection |
| Home-screen widget | NOT STORE-RELEASED | `docs/features/home-screen-widget.md:3-17` — status "implemented; release validation pending"; "native delivery is in development builds, not a store release." | doc-evidence |

## 1. Claims from PLAN.md §1.1 (re-verified, not re-derived)

Every path below was opened in this pass; line numbers are as found in this checkout on
2026-09-20 and may drift by a line or two from PLAN.md's own citation as the docs evolve —
each was spot-checked and the cited text was found at or within a few lines of the stated
range.

| Claim | Verdict | Evidence path:lines | Evidence class | Marketing caveat |
|---|---|---|---|---|
| Camera-roll scan with permission, 3-hour-gap clustering into suggested memories | TRUE | `docs/features/gallery-import.md:16-24` (background sweep, windowed enumeration), `:89` (`GALLERY_IMPORT_CLUSTER_GAP_MS`, 3-hour gap, unchanged from before the continuous model); `src/utils/gallery-import-scanner.ts` (exists, not exhaustively re-read this pass) | code-evidence + doc-evidence | Describe as "groups your photos into moments" — do not claim it scans the whole cloud library instantly; it is a windowed, foreground/background-while-open sweep. |
| AI drafts captions per cluster (`gpt-4o-mini`, Cloudflare `GalleryImportWorkflow`) | TRUE | `docs/features/gallery-import.md:77` (`GalleryImportWorkflow` in `cloudflare/memory-illustration-worker` ... calls `gpt-4o-mini`) | doc-evidence | Do not claim on-device/private-only processing — previews leave the device to a vision model. |
| Approval control is literally Keep / Set aside; nothing auto-approves | TRUE | `docs/features/gallery-import.md:31` — "Keep proceeds to a composer... Set aside is a recoverable in-run skip. No card is auto-approved or automatically resurfaced later." | doc-evidence | Use "Keep" / "Set aside" verbatim if showing UI controls in a screenshot. |
| 512px previews upload to private R2 for AI before review; full originals only after approval | TRUE | `docs/features/gallery-import.md:10` (overview: "uploads downscaled private previews before review... only final approval uploads and retains full-resolution originals") | doc-evidence | Disclose that small previews are uploaded before review; do not claim zero upload before approval. |
| Camera roll never modified | TRUE | `docs/features/gallery-import.md:10` — "Momora never changes the camera roll." | doc-evidence | Safe as stated. |
| Gallery import enabled in the production build profile | TRUE | `eas.json:49` — `"env": { "EXPO_PUBLIC_GALLERY_IMPORT_ENABLED": "true" }` inside the `production` build profile; `src/utils/gallery-import-flags.ts:7` reads that exact env var | config-evidence | Client flag only — see next row for the separate server gate, which is unverified. |
| Gallery import also has a server-side admission gate | TRUE (existence of the gate) | `docs/features/gallery-import.md:151` — "`EXPO_PUBLIC_GALLERY_IMPORT_ENABLED` ... enables client entry points... The database singleton's `enabled` field separately controls new server admission" | doc-evidence | Whether that server flag is currently `true` in the production database is **unverified from this checkout** — see BLOCKERS.md. |
| Typed short note; dictation transcribes then discards audio | TRUE | `docs/features/memories.md` (feature description, not independently re-opened at :344 this pass — see next row for the line that was directly read); `docs/features/voice-journaling.md:13,83` (exists, not re-opened line-by-line this pass) | doc-evidence | Never imply dictation retains audio. |
| "Keep the sound" persists the actual clip as the memory artifact | TRUE | `docs/features/audio-memories.md:11` (overview: "keep the recording itself as the keepsake") and `:16` (design decision 2: "Dictation always transcribes and always discards audio... If someone wants to preserve a recording, the answer is always 'keep the sound.' Exactly one kind of kept audio exists in the product.") | doc-evidence | Safe as stated; do not imply every dictated note keeps audio. |
| Saved sound plays back with visible elapsed/total duration and an editable caption | TRUE | `docs/features/audio-memories.md:17` (design decision 3: description is user-editable "like a media caption") and data model section (`duration_ms populated`) | doc-evidence | Safe as stated. |
| Text memory → optional AI illustration using date-resolved family portraits | TRUE | `docs/features/memories.md:20-28` (illustration cap/toggle mechanics), `:325-330` (portrait date-resolution order: "latest ready portrait on/before `memory_date`, then earliest ready after it, then an undated migrated legacy portrait") | doc-evidence | Illustration is optional and text-triggered, not automatic on every memory. |
| Gallery import never produces an illustration; photo and illustration pipelines are mutually exclusive | TRUE | `docs/features/memories.md:24` ("Attach one or more photos/videos → `media` type; AI toggle is hidden.") and `docs/features/gallery-import.md` overview (imported photos become ordinary private `media` memories, not `text_illustration`) | doc-evidence | Never show a screenshot implying gallery import produced a watercolor illustration. |
| Illustration caps at 6 tagged members and needs a ready portrait | TRUE | `docs/features/memories.md:20` ("AI illustrations support at most 6 tagged members") and `:327` ("Illustration requires a usable date-resolved portrait for at least one selected character (`NO_PORTRAITS` otherwise)") | doc-evidence | Safe as stated. |
| Roles are exactly owner / manager / viewer | TRUE | `docs/features/family-sharing.md:11` ("one of three roles (owner / manager / viewer)") | doc-evidence | Do not invent a fourth role or "admin" language. |
| Viewers cannot write memories, invite, export, or order a book | TRUE | `docs/features/family-sharing.md:74-90` capability table — viewer row is `✗` for create/edit/delete memories, invites, and (per `docs/features/memory-book-generation.md`, not re-opened for this exact row this pass) book ordering; export is owner-only per `docs/features/data-export.md:12` ("Settings exposes Export your memories only to a family owner") | doc-evidence | Safe as stated. |
| Invite = 3-word dash-separated code, shared out-of-band, then owner/manager approval | TRUE | `docs/features/family-sharing.md:15-18` (share sheet with universal link + raw code; "enter the 3-word code (dash- or space-separated, case-insensitive)") and `:257-270` (invite lifecycle: `pending → redeemed → approved/rejected`, approval by owner/manager) | doc-evidence | Safe as stated; do not show or imply an in-app QR/contact-picker flow not described here. |
| Timeline, calendar, per-child profile collection, memory detail all exist | TRUE | Routes confirmed to exist on disk: `app/(app)/(tabs)/timeline.tsx`, `app/(app)/(tabs)/calendar.tsx`, `app/(app)/family/[id]/index.tsx`, `app/(app)/memory/[id]/index.tsx` | code-evidence (file existence only — screens' populated states not captured, see ASSET-CHECKLIST.md) | — |
| Book: choose child + scope (age-year / calendar-year / everything), AI curates outline, lays out pages | TRUE | `docs/features/memory-book-generation.md:24-38` (overview: "parent-initiated request to turn a chosen time period... into a premium AI-curated, AI-laid-out hardcover book"; dispatcher + Cloudflare Workflow curates outline and assembles `book_document`) | doc-evidence | Safe as stated; the in-app scope-picker UI and web preview are explicitly a separate, later slice ("5b/5c") per the same doc — verify they ship before implying an in-app picker screenshot. |
| Book review/edit/order happens in an external system browser at `shop.usemomora.com`, separate login | TRUE | `docs/features/memory-book-generation.md:60-63` — "`shop.usemomora.com/b/<id>` in the system browser — the web app has its own (separate) login... The web app polls `status` and renders the preview once `ready`." | doc-evidence | Never show or imply an in-app WebView or native page editor. |
| Print is a separate Stripe purchase, not part of the subscription | TRUE | `docs/features/memory-book-orders.md:233-237` — Stripe Checkout Session (`STRIPE_SECRET_KEY`) and a distinct `PRICE_USD_CENTS` env var described as "our flat price; owner sets once the physical sample is priced" | doc-evidence | Never claim a bundled or included print product. |
| Whether book generation/online review is subscription-included | UNVERIFIED | Not stated in `docs/features/memory-book-generation.md` or `memory-book-orders.md`; `momora-listing-handoff-v2/approved-listing-copy.json` `commercial_context.book_generation_and_online_review_entitlement` itself says `"verify_before_claiming_included_or_free"` | doc-evidence (absence of a statement) | Copy must not call book generation/review "included" or "free" until this is answered — see COMMERCIAL-EXPECTATION-MAP.md and BLOCKERS.md. |
| Subscription gates new memory writes, engagement writes, media changes and AI generation — not reading | TRUE | `docs/features/subscriptions.md:39-42` — "Owners with an expired subscription can browse and export the archive, but new memory writes, likes/comments, media changes, and AI generation are gated behind a resubscribe paywall." | doc-evidence | Safe as stated. |
| Lapsed owner keeps browse + export | TRUE | `docs/features/subscriptions.md:39` (same passage above) and `docs/features/data-export.md:9-12` — "Export is deliberately available forever, including after subscription lapse" and `:108-109` — "Export never uses RevenueCat access checks; a lapsed owner must still be able to export." | doc-evidence | Safe as stated; do not imply this extends to new writes or AI. |
| Export is owner-only, ZIP with `manifest.json` + numbered assets, owner-owned families only | TRUE | `docs/features/data-export.md:9-11` (ZIP archive contents), `:17` ("Settings exposes Export your memories only to a family owner"), `:20-21` (structured `manifest.json` plus numbered files), `:60-61` ("Family members who are not owners cannot export another household's archive.") | doc-evidence | Safe as stated. |

## 2. Listing-relevant claims implied by the v2 description and seven frames

Source: `momora-listing-handoff-v2/approved-listing-copy.json` `description` field and the
`screenshots` array (ids `01-more-than-photos` through `07-look-back`).

| Implied claim | Verdict | Evidence path:lines | Evidence class | Marketing caveat |
|---|---|---|---|---|
| "Momora groups photos and drafts captions for you to review" (promotional_text) | TRUE | Same gallery-import evidence rows above | doc-evidence | Already covered above. |
| "You can turn a story into an AI illustration, too, using your family's character portraits" (description, FOR THE THINGS A PHOTO MISSED section) | TRUE | `docs/features/memories.md:20-28,307-336` | doc-evidence | Already covered above. |
| "Save the recording itself and listen again — not just a transcript" (description, KEEP THEIR LITTLE VOICE section) | TRUE | `docs/features/audio-memories.md:11,16` | doc-evidence | Already covered above. |
| "Keep memories together for each child. Look back through the photos and stories... invite family members" (description, SOMETHING TO COME BACK TO section) | TRUE for per-child collection and invites; the phrase "Look back" here is generic English, not a reference to the unreleased Looking Back feature | `app/(app)/family/[id]/index.tsx` exists (per-child route); `docs/features/family-sharing.md:11` (invites) | code-evidence + doc-evidence | The word "look back" in lowercase prose is fine; the composed screenshot for frame 07 must not visually reuse the Looking Back carousel asset (`back-on-timeline.png`) — see Never-claim section above. |
| "Review the book online, make changes and order a printed copy delivered to your door" (description, AND A BOOK TO HOLD section) | TRUE for online review via external browser; "make changes" scope and delivery specifics not verified in this pass | `docs/features/memory-book-generation.md:60-63` | doc-evidence | Do not imply in-app native editing; "make changes" should map to whatever the `shop.usemomora.com` web app actually allows, which was not inspected beyond the routing/login facts above. |
| "Your saved memories remain available to read after your subscription ends. The family owner can export the archive, too." (description, YOUR MEMORIES STAY YOURS section) | TRUE | `docs/features/subscriptions.md:39`, `docs/features/data-export.md:9-12` | doc-evidence | Already covered above. |
| Frame 02 — "the actual gallery suggestion deck... a complete grounded draft caption and the current Keep / Set aside controls" | TRUE (feature exists); genuine screen capture not yet on disk | `docs/features/gallery-import.md:31`; routes `app/(app)/gallery-import/{index,review,progress,approve}.tsx` confirmed to exist | doc-evidence + code-evidence (routes only) | See ASSET-CHECKLIST.md frame 02 — `blocked` on capture, not on feature existence. |
| Frame 04 — "The real saved sound-memory player with a legitimate demo clip, duration and a short description" | TRUE (feature exists); genuine screen capture not yet on disk. `SoundCard` component confirmed at `src/components/memory-card.tsx` (file exists; not opened line-by-line this pass) | `docs/features/audio-memories.md` overview + data model sections | doc-evidence + code-evidence (file existence) | See ASSET-CHECKLIST.md frame 04 — `blocked` on capture. |
| Frame 06 — "Actual invitation/member controls... Respect viewer/manager roles" | TRUE (feature exists); the only capture on disk (`IMG_0909.PNG`, character roster) shows the illustration cast, not the invite/roles UI | `docs/features/family-sharing.md:74-90`; routes `app/(app)/sharing/{members,invite,pending-invites,approvals,redeem}.tsx` confirmed to exist | doc-evidence + code-evidence (routes only) | See ASSET-CHECKLIST.md frame 06 — `partial`, roster ≠ roles/invites. |
